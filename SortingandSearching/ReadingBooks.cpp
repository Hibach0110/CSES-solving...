#include <bits/stdc++.h>
using namespace std;

bool used[200005];

int main() {
     ios_base::sync_with_stdio(0); cin.tie(0);
     
     int N; cin >> N;
     vector<int> a(N);
     for (int &x : a) cin >> x;
     

     
     return 0;
}
